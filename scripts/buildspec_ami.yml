version: 0.2

phases:
  install:
    commands:
      - echo Nothing in install phase

  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...

  build:
    commands:
      - echo Build started on `date`

  post_build:
    commands:
      - echo Build completed on `date`
      
      - sed -i 's|he input file is password protected and cannot be processed.|. Use the \"Password\" option to provide the password.
artifacts:
  files:
    - ./**/*